alias hextostr="perl -pe 's/(..)/chr(hex($1))/ge'"
alias files='find . -type f | grep -v /\.svn | grep -v /config | grep -v /build | grep -v /\.gradle'
function savealias() { echo "alias $1" >> ~/.bashrc && source ~/.bashrc; }
